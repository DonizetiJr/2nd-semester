#include <stdlib.h>
#include <stdio.h>
#include <listaestatica.h>
#include <item.h>

int main(int argc, char *argv[]) {
  LISTA_ESTATICA *lista = criar_lista(10);

  for (int i = 0; i < 10; i++) {
    ITEM *item = criar_item(i,i);
    inserir_lista_estatica(lista,item);
  }

  inverter_lista(lista);
  imprimir_lista(lista);
  
  apagar_lista(&lista);

  return 0;
}
